window.onload = function () {
  //console.log = ("loaded")
  var typed = new Typed('#typed', {
    strings: ["Welcome to my profile"],
    backspeed: 15,
    smartbackspace : true,
    backDelay: 1200,
    startDelay: 1000,
    typespeed: 25,
    loop: true,
  });
};
